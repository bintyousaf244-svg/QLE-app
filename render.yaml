services:
  - type: web
    name: qle-app
    runtime: node
    plan: free
    buildCommand: cd artifacts/api-server && pnpm install && pnpm run build
    startCommand: cd artifacts/api-server && pnpm run start
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres.znntitoiilpsahhdrvyv:QuihItn4kCXtgkxb@aws-1-ap-south-1.pooler.supabase.com:6543/postgres
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
